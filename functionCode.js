 function nameDisplay() {
     let name = 'abhsihek';
     console.log(name);
 }
 //nameDisplay();

 // Outer variable code for function.

 let userName = "kumar";

 function dis() {
     let name = "abhishek";
     let message = `${name}  ${userName}`;
     console.log(message);

 }

 dis();
 //in thi we can access outer vaible but we can't access inner vaiable to outer works.